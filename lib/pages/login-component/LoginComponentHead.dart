import 'package:flutter/material.dart';

class LoginComponentHead extends StatelessWidget {
  const LoginComponentHead({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
