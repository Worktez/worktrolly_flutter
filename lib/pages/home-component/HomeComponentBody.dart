import 'package:flutter/material.dart';

class HomeComponentBody extends StatelessWidget {
  const HomeComponentBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Home Body..."),
    );
  }
}
